import Workspace from '@layouts/Workspace';
import React from 'react';

const Channel = () => {
  return (
    <Workspace>
      <div>
        channel
      </div>
    </Workspace>
  );
};

export default Channel;